---
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
      # Override the base podman default so shared prepare writes the
      # telemt.test entry into the runner's real /etc/hosts.
      mp_driver: native
